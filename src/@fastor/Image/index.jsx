import styled from 'styled-components'
import { Base, Space, Typography } from '../Common'

export const Figure = styled.figure `
    ${Base}
`

export const FigCaption = styled.figcaption`
    ${Typography}
    ${Space}
`
