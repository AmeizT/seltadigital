import React from 'react'
import { IconContext } from 'react-icons'
import { Button, Container, Drawer, Item, List, ListItem, NavLink, Segment, Stack, Text } from '../../kui'

export default function AppDrawa({ navi, setNavi }){
    return(
        <Drawer
        ht="100vh"
        space="var(--bar10) 0"
        device="phone"
        of="hidden scroll"
        paint="var(--snow10)"
        paintd="var(--dark70)">
            <Container ht="100%" dn="column">
                
            </Container>
        </Drawer>
    )
}